{
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "scripts": {
    "dev": "yarn workspace @app/server run dev",
    "build": "yarn workspaces foreach run build"
  },
  "workspaces": [
    "app/*",
    "libs/*"
  ],
  "devDependencies": {
    "typescript": "4.7.4"
  }
}
