{
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "scripts": {
    "build": "yarn workspace @app/server run build",
    "serve": "yarn workspace @app/server run serve",
    "dev": "yarn workspace @app/server run dev"
  },
  "workspaces": [
    "app/*",
    "libs/*"
  ],
  "devDependencies": {
    "typescript": "4.7.4"
  }
}
